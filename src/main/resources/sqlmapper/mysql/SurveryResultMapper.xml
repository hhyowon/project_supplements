<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="SurveyResult">
    
    <!-- 설문 결과  -->

    <select id="surveyresult_no" parameterType="Map" resultType="Map">
    SELECT COUNT(*)
    FROM SURVEY
    WHERE SURVEY_OPT_ID <![CDATA[<]]> "O-25"
    GROUP BY  SURVEY_OPT_ID
	ORDER BY SURVEY_OPT_ID  ASC
    ;
	</select>

      <select id="surveyresult_yes" parameterType="Map" resultType="Map">
    SELECT COUNT(*)
    FROM SURVEY
    WHERE SURVEY_OPT_ID <![CDATA[>]]> "O-24"
    GROUP BY  SURVEY_OPT_ID
	ORDER BY SURVEY_OPT_ID  ASC
    ;
	</select>



    
</mapper>



 